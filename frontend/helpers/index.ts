export const intersectFadeInOut = {
    class: ['animate__animated', 'delay-500'],
    false: ['animate__fadeOut'],
    true: ['animate__fadeIn']
}

export const imgCarousel = {
    modifiers: { width: 200 },
    options: { format: "png" }
}

export const imgLinkbox = {
    modifiers: { width: 200 },
    options: { format: "png" },
}